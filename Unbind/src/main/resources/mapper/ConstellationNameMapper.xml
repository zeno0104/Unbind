<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.unbind.mapper.ConstellationNameMapper">

	<select id="findAllByUserId" parameterType="long"
		resultType="com.example.unbind.domain.ConstellationName">
		SELECT id, user_id, tag, name
		FROM constellation
		WHERE user_id = #{userId}
	</select>

	<update id="upsert">
		INSERT INTO constellation (id, user_id, tag, name)
		VALUES (nextval('constellation_seq'), #{userId}, #{tag}, #{name})
		ON CONFLICT (user_id, tag) DO UPDATE SET name = EXCLUDED.name
	</update>

</mapper>
