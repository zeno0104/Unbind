<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.unbind.mapper.PushSubscriptionMapper">

	<insert id="upsert" parameterType="com.example.unbind.domain.PushSubscription">
		<selectKey keyProperty="id" resultType="long" order="BEFORE">
			SELECT nextval('push_subscription_seq')
		</selectKey>
		INSERT INTO push_subscription (id, user_id, endpoint, p256dh, auth, created_at)
		VALUES (#{id}, #{userId}, #{endpoint}, #{p256dh}, #{auth}, CURRENT_TIMESTAMP)
		ON CONFLICT (endpoint) DO UPDATE SET
			user_id = EXCLUDED.user_id,
			p256dh = EXCLUDED.p256dh,
			auth = EXCLUDED.auth
	</insert>

	<delete id="deleteByEndpoint">
		DELETE FROM push_subscription WHERE endpoint = #{endpoint}
	</delete>

	<delete id="deleteById">
		DELETE FROM push_subscription WHERE id = #{id}
	</delete>

	<delete id="deleteAllByUserId">
		DELETE FROM push_subscription WHERE user_id = #{userId}
	</delete>

	<select id="findForPendingReminders" resultType="com.example.unbind.domain.PushSubscription">
		SELECT DISTINCT ps.id, ps.user_id, ps.endpoint, ps.p256dh, ps.auth
		FROM push_subscription ps
		JOIN journal_entry je ON je.user_id = ps.user_id
		JOIN action_item ai ON ai.entry_id = je.id
		WHERE ai.is_completed = 0
		  AND je.created_at &lt; CURRENT_TIMESTAMP - (#{days} * INTERVAL '1 day')
	</select>

</mapper>
